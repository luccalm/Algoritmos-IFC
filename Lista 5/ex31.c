/* 31) Elaborar um algoritmo que lê duas matrizes M(4,6) e N(4,6) e cria uma matriz que
seja:
a. o produto de M por N;
b. a soma de M com N;
c. a diferença de M com N;
Escrever as matrizes calculadas. */